{
  "name": "safety-topic",
  "version": "1.0.0",
  "description": "Quick, credible safety briefs for meeting openers",
  "scripts": {
    "dev": "node --watch --watch-path . server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.4",
    "jsdom": "^29.0.2",
    "vitest": "^4.1.4"
  }
}
